var staff = require('./staff');
var team = require('./team');
var global  = require('./global');
var data  = require('./data');

module.exports = {
  staff: staff,
  team: team,
  global: global,
  data: data
}
